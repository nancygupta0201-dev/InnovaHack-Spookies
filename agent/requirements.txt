# --- Web server ---
fastapi==0.139.0
uvicorn==0.50.2
python-multipart==0.0.32          # required by FastAPI for File()/Form() uploads

# --- Agent framework ---
google-adk==2.3.0
google-genai==2.10.0              # google.genai.types (Content/Part) used directly

# --- LiteLLM (Claude via ADK) ---
# NOT present in your current freeze — this is why the LiteLLM ImportError
# happened earlier. Install with: pip install "google-adk[extensions]"
# litellm==<version>              # <- pin once installed, see note below

# --- MCP (Tavily / GitHub tool servers) ---
mcp==1.28.1

# --- PDF parsing ---
pypdf==6.14.2

# --- Config / env ---
python-dotenv==1.2.2

# --- Data models ---
pydantic==2.13.4

# --- HTTP client (used in your test/client scripts) ---
requests==2.34.2
